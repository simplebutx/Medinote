server:
  port: 8082

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  jackson:
    time-zone: Asia/Seoul

ai:
  service:
    base-url: ${AI_SERVICE_BASE_URL:http://localhost:8000}

medication:
  service:
    base-url: ${MEDICATION_SERVICE_BASE_URL:http://localhost:8081}

